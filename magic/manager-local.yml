server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8101/eureka/
spring:
  application:
    name: manager
  profiles:
    active: local
feign:
  hystrix:
    enable: true
